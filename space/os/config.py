# space/os/config.py

DEFAULT_CONFIG = {
    "app_name": "space",
    "database_root_dir": None, # Base directory for all app databases. Defaults to PROJECT_ROOT/.space if None.
    "logging_level": "INFO",
    "debug_mode": False,
}